<h1>Catalogue</h1>

<h2><%= user_signed_in? ? (link_to 'New', new_reel_path, class: 'btn btn-primary') : '' %></h2>

<% @reels.each do |reel| %>
  <p>
    <%= link_to reel.brand.brand, reel %>
    <%= link_to reel.item_name, reel %>
  </p>
<% end %>